export type ButtonProps = {
  text: string;
};
export type allmicrocmsWorksProps = {
  githubUrl: string;
  productDatail: string;
  productImage: { url: string };
  productTitle: string;
  productUrl: string;
  productComposition: string;
  productData: Date;
};

export type IconProps = {
  iconImageUrl: string;
  marginLeft: number;
};

export type allmicrocmsDailyuiProps = {
  figma: string;
  title: string;
  image: string;
};
export type allmicrocmsSlideProps = {
  slideTitle: string;
  slideUrl: string;
};
